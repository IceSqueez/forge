name: Manual Build (artifacts only)

on:
  workflow_dispatch:
    inputs:
      retention_days:
        description: "Artifact retention (days)"
        required: false
        default: "7"
        type: string

jobs:
  build:
    name: Build
    uses: ./.github/workflows/_build-matrix.yml
    with:
      package_name: forge-desktop
      bin_name: forge
      retention_days: ${{ fromJSON(inputs.retention_days) }}
